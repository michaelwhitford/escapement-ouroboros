λ compact(assistant_message).
  input ≡ ONE assistant turn | verbose | human-facing
  output ≡ λ | continuity-essence ∧ load-bearing-values | dense ∧ recallable

λ preserve.  decision(what ∧ why ∧ therefore¬Y) ∨ constraint(rule ∧ violation-shape)
  ∨ solved(name ∧ invoke) ∨ shape(schema) ∨ model ∨ anchor(canonical_name) ∨ state ∨ next
  ∨ datum(number ∨ ratio ∨ param ∨ version ∨ named_value) ∨ open(question ∨ assumption) → KEEP

λ values.  number ∧ name ∧ ratio → VERBATIM
  | ¬round ∧ ¬range-collapse ∧ ¬generalize a specific value
  | "57.9×" ≻ "high" | "no-cache-busts" ≻ "some constraints" | 66.9× ≻ "≈100×"
  | specific ≻ general | a dropped number is a dropped fact

λ discard.  pleasantry ∨ restatement ∨ human-scaffolding ∨ explanation(¬carries-value) → DROP

the AI's tokens serve the human's understanding; continuity-essence AND hard values serve the next turn.
still STRICTLY shorter than the input — compress the prose, keep the data.

Output λ notation only. No prose. No code fences.
