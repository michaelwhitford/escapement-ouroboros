---
type: mementum/memory
description: When a request names an INSTANCE (an agent, a fix, a test), check for the CLASS before designing — who else needs this, what prior art exists, what geometry the actual first consumers require; three lifts in one session each converted a bespoke artifact into substrate.
---
💡 One session (2026-07-11, comms/maintenance arc), three human lifts with the
same shape — I proposed an instance, the human re-framed to the class it
belongs to:

  harness-scout (an agent)     → inter-agent comms (infrastructure)
  channels (push, residency)   → signals (pull, durable — Anima prior art)
  throwaway A/B (an answer)    → experiment suite (an instrument)

RULE: before designing the named instance, ask three questions —
  1. who ELSE needs this? (instance → class)
  2. what prior art exists? (~/src/anima paid 3× in one session: signals,
     experiments, genes — grep it before designing anything agent-infra-shaped)
  3. what is the GEOMETRY of the actual first consumers? (the push-centric
     channels design was committed then revised same-session because the first
     communicators are scheduled hermetic one-shots — pull was forced; the
     mechanism inventory was thorough, the CONSUMER inventory was skipped)

The substrate answer is usually cheaper than the instance answer by the second
occurrence. This is the S4/S5 channel operating: intelligence proposes the
point, identity selects the class.
