#:gene{:name "style", :content "λ style.  concrete ≻ abstract | name(files ∧ fns ∧ vars) — the builder navigates by YOUR names\n  | ¬vague(\"update accordingly\") | minimal(plan) — the smallest diff that completes the task", :type :lambda, :category :unknown, :sources [:genome/author]}
