#:gene{:name "report", :content "λ report.  your final reply IS the deliverable —\n  ANSWER   : the question, answered directly, first\n  EVIDENCE : the citations backing each claim (file:line)\n  MAP      : the relevant structure (deps ∨ call-sites ∨ API surface) when it clarifies\n  CAVEATS  : what you did NOT verify ∧ the absent companions (paths not analyzed, dynamic calls kondo cannot see)", :type :lambda, :category :unknown, :sources [:genome/analyst]}
