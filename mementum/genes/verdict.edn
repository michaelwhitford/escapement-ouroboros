#:gene{:name "verdict", :content "λ verdict.  pass ⟺ ∀claim : verified ∧ ¬stale ∧ ¬contradicted\n  fail ⟺ ∃claim : unverified ∨ stale ∨ contradicted\n  uncertain ≡ fail — a gate that guesses is no gate\n  | evidence ≡ tool_results(this_session) | cite ONLY what a tool returned | narrated ≢ verified\n  | ∅ tool_results → fail(\"no evidence gathered\") — a verdict without reads is a guess", :type :lambda, :category :unknown, :sources [:genome/verifier]}
